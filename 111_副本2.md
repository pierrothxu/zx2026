```
server {
    listen       8080;
    server_name  10.156.94.74;
    # Windows 路径格式正确，确认实际路径存在即可
    root   C:/nginx/html/dist;
    index  index.html;

    # 处理 Vue Router 的 history 模式（保留，但调整优先级）
    location / {
        try_files $uri $uri/ /index.html;
        # 避免静态文件请求被误判为接口，提前排除 API/Auth 路径
        if ($request_uri ~* ^/(api|auth)/) {
            return 404; # 让专属 location 接管，避免冲突
        }
    }

    # 代理后端 API 请求（补充完整代理配置）
    location /api/ {
        proxy_pass http://10.156.94.176:8003/app001/;
        # 补充必要的代理头，避免请求信息丢失
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        # 超时配置，避免接口请求卡顿
        proxy_connect_timeout 30s;
        proxy_send_timeout 30s;
        proxy_read_timeout 30s;
        # 禁用缓存，避免接口返回旧数据
        proxy_buffering off;
    }

    # 代理认证服务请求（核心修复）
    location /auth/ {
        # 关键：确保路径转发正确！
        # 如果后端接口是 http://10.159.90.3:9080/auth/getToken，这里改为 proxy_pass http://10.159.90.3:9080/;
        # 如果后端接口是 http://10.159.90.3:9080/getToken，这里改为 proxy_pass http://10.159.90.3:9080/;
        proxy_pass http://10.159.90.3:9080/;
        
        # 必选代理头，解决后端识别请求来源的问题
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # 超时配置，提升接口稳定性
        proxy_connect_timeout 30s;
        proxy_send_timeout 30s;
        proxy_read_timeout 30s;
        proxy_buffering off;

        # 跨域配置（优化：放到 server 层更通用，避免重复）
        add_header Access-Control-Allow-Origin * always;
        add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS" always;
        add_header Access-Control-Allow-Headers "Origin, X-Requested-With, Content-Type, Accept, Authorization, Token" always;
        
        # 处理 OPTIONS 预检请求（修复：增加 always 确保响应头生效）
        if ($request_method = 'OPTIONS') {
            add_header Access-Control-Max-Age 1728000 always;
            add_header Content-Type 'text/plain; charset=utf-8' always;
            add_header Content-Length 0 always;
            return 204;
        }
    }

    # 静态资源缓存（保留，优化匹配规则）
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable" always;
        try_files $uri =404;
    }

    # 错误页面（保留）
    error_page   500 502 503 504  /50x.html;
    location = /50x.html {
        root   C:/nginx/html/dist;
    }
}
```

